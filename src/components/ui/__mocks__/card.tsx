import React from 'react';

export const Card = ({ children }: any) => children;
export const CardHeader = ({ children }: any) => children;
export const CardTitle = ({ children }: any) => children;
export const CardContent = ({ children }: any) => children;